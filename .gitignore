.DS_Store
*.swp
*.swo
*~
.netrwhist
.luarc.json
tags

# Tmux plugins. Nothing installs them any more (the tpm declarations were dead
# and are gone from tmux.conf), but the path stays ignored so a stray clone
# never gets committed again the way six empty gitlinks once were.
configs/tmux/.config/tmux/plugins/

# Runtime logs
*.log
